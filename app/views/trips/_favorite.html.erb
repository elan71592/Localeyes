<%= form_for(Favorite.new, html: {class: "favorite-submit"} ) do |f| %>
  <div><%= hidden_field_tag "trip", "#{trip.id}" %></div>
  <%= f.submit "Favorite", class: "waves-effect waves-light btn" %>
<% end %>
<br>