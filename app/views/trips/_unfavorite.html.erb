<%= form_for current_user.favorites.find_by(trip_id: trip.id), html: { method: :delete, class: "unfavorite-submit"} do |f| %>
  <div><%= hidden_field_tag "trip_id", "#{trip.id}" %></div>
  <%= button_tag(type: 'submit', class: "favorite-button") do %>
  <i class="medium material-icons amber-text text-accent-4">star</i>
  <h6><%= @trip.favorites.count %> favorites</h6>
  <% end %>
<% end %>